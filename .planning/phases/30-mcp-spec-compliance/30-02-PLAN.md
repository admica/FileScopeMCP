---
phase: 30-mcp-spec-compliance
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/mcp-server.test.ts
  - tests/unit/tool-outputs.test.ts
autonomous: true
requirements:
  - SPEC-01

must_haves:
  truths:
    - "Test suite passes with all 13 tool names verified against registerTool pattern"
    - "No test asserts server.tool( string — all updated to server.registerTool("
    - "npm test exits 0 with no failures"
  artifacts:
    - path: "src/mcp-server.test.ts"
      provides: "COMPAT-01 tool name registry test updated for registerTool API"
      contains: "server.registerTool"
    - path: "tests/unit/tool-outputs.test.ts"
      provides: "Tool name registry test updated for registerTool API"
      contains: "server.registerTool"
  key_links:
    - from: "src/mcp-server.test.ts line 537"
      to: "src/mcp-server.ts registerTool calls"
      via: "static source grep assertion"
      pattern: 'server.registerTool\("'
    - from: "tests/unit/tool-outputs.test.ts line 444"
      to: "src/mcp-server.ts registerTool calls"
      via: "static source grep assertion"
      pattern: 'server.registerTool\("'
---

<objective>
Update two test files that verify MCP tool registration by grepping source code for the `server.tool("name"` pattern. After Plan 01 migrated all 13 tools to `server.registerTool("name"`, these assertions fail. Update both test files to check for the new pattern and verify the entire test suite passes.

Purpose: Ensure the tool name registry tests correctly validate the migrated registerTool API and the full test suite is green.
Output: Both test files updated, npm test passes with zero failures.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mcp-spec-compliance/30-PATTERNS.md

<interfaces>
<!-- The assertion pattern that needs updating in both test files -->

Current (broken after Plan 01):
```typescript
expect(src).toContain(`server.tool("${toolName}"`);
```

Target (matches Plan 01 migration):
```typescript
expect(src).toContain(`server.registerTool("${toolName}"`);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tool registration assertions in both test files</name>
  <files>src/mcp-server.test.ts, tests/unit/tool-outputs.test.ts</files>
  <read_first>
    - src/mcp-server.test.ts (lines 507-539, the COMPAT-01 test that greps for server.tool pattern)
    - tests/unit/tool-outputs.test.ts (lines 416-447, the MCP tool name registry test)
    - src/mcp-server.ts (verify Plan 01 completed — confirm registerTool calls exist)
  </read_first>
  <action>
**In src/mcp-server.test.ts, line 537:**

Change:
```typescript
expect(src).toContain(`server.tool("${toolName}"`);
```
To:
```typescript
expect(src).toContain(`server.registerTool("${toolName}"`);
```

This is the only change needed in this file. The test reads src/mcp-server.ts as text and checks that each of the 13 expected tool names appears in a registration call. After Plan 01 migrated all tools to registerTool(), the string pattern must match.

**In tests/unit/tool-outputs.test.ts, line 444:**

Change:
```typescript
expect(src).toContain(`server.tool("${tool}"`);
```
To:
```typescript
expect(src).toContain(`server.registerTool("${tool}"`);
```

Same single-line change. This test is structurally identical to the one in mcp-server.test.ts — it reads the source and verifies all 13 tool names are registered.

No other changes needed in either test file. The tool response shape tests (staleness injection, search contract, etc.) are testing response construction logic independent of the registration API — they will continue to pass as-is.
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/mcp-server.test.ts contains `server.registerTool("${toolName}"` (not `server.tool(`)
    - tests/unit/tool-outputs.test.ts contains `server.registerTool("${tool}"` (not `server.tool(`)
    - `grep -c 'server\.tool(' src/mcp-server.test.ts` returns 0
    - `grep -c 'server\.tool(' tests/unit/tool-outputs.test.ts` returns 0
    - `npm test` exits 0 with all tests passing
    - `npm run build` exits 0 (full build succeeds)
  </acceptance_criteria>
  <done>Both test files assert server.registerTool pattern. npm test passes with zero failures. npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only changes — no runtime trust boundaries affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | test assertions | accept | Test changes are string literal updates only. No security implications — tests verify source structure, not runtime behavior. |
</threat_model>

<verification>
1. `npm test` exits 0 — all tests pass
2. `npm run build` exits 0 — full build succeeds
3. `grep 'server\.tool(' src/mcp-server.test.ts tests/unit/tool-outputs.test.ts` returns no matches
4. `grep 'server\.registerTool(' src/mcp-server.test.ts tests/unit/tool-outputs.test.ts` returns 2 matches (one per file)
</verification>

<success_criteria>
Both test files updated to match registerTool API pattern. Full test suite passes. Full build succeeds. Zero references to deprecated server.tool( remain in test assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-spec-compliance/30-02-SUMMARY.md`
</output>
