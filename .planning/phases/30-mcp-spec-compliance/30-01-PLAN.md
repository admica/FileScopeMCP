---
phase: 30-mcp-spec-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp-server.ts
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04

must_haves:
  truths:
    - "All 13 tools are registered via server.registerTool() — zero server.tool() calls remain"
    - "listChanged capability is not declared in user-space McpServer constructor options"
    - "Every tool has explicit annotations (readOnlyHint, destructiveHint, idempotentHint, openWorldHint)"
    - "Error responses are structured JSON with ok:false, error code, and message"
    - "Success responses are structured JSON with ok:true and spread data"
  artifacts:
    - path: "src/mcp-server.ts"
      provides: "All 13 MCP tool registrations via registerTool API with annotations and structured responses"
      contains: "server.registerTool"
  key_links:
    - from: "src/mcp-server.ts mcpError()"
      to: "ToolResponse type"
      via: "returns { content: [{type:'text', text: JSON.stringify({ok:false,...})}], isError: true }"
      pattern: "mcpError"
    - from: "src/mcp-server.ts mcpSuccess()"
      to: "ToolResponse type"
      via: "returns { content: [{type:'text', text: JSON.stringify({ok:true,...})}] }"
      pattern: "mcpSuccess"
    - from: "server.registerTool config.annotations"
      to: "ToolAnnotations interface"
      via: "readOnlyHint, destructiveHint, idempotentHint, openWorldHint fields"
      pattern: "annotations:"
---

<objective>
Migrate all 13 MCP tool registrations from deprecated server.tool() to server.registerTool() with config objects containing title, enriched descriptions, inputSchema (raw Zod shapes), and explicit ToolAnnotations. Replace createMcpResponse() with mcpError()/mcpSuccess() helpers for structured JSON responses. Remove false listChanged capability from constructor.

Purpose: Achieve full MCP spec compliance — tools get correct annotations so MCP clients can make informed safety decisions, and LLM agents can parse structured error codes programmatically.
Output: Fully migrated src/mcp-server.ts with zero server.tool() calls, all 13 tools annotated, and uniform { ok: true/false } response format.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mcp-spec-compliance/30-CONTEXT.md
@.planning/phases/30-mcp-spec-compliance/30-RESEARCH.md
@.planning/phases/30-mcp-spec-compliance/30-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types.ts:
```typescript
export interface ToolResponse {
  content: Array<
    | { type: "text"; text: string; [key: string]: unknown }
    | { type: "image"; mimeType: string; data: string; [key: string]: unknown }
    | { type: "resource"; resource: { uri: string; text: string; mimeType?: string; [key: string]: unknown }; [key: string]: unknown }
  >;
  _meta?: { [key: string]: unknown };
  isError?: boolean;
  [key: string]: unknown;
}
```

From SDK (node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.d.ts:150-157):
```typescript
server.registerTool(
  name: string,
  config: {
    title?: string;
    description?: string;
    inputSchema?: ZodRawShapeCompat | AnySchema;
    outputSchema?: ZodRawShapeCompat | AnySchema;
    annotations?: ToolAnnotations;
    _meta?: Record<string, unknown>;
  },
  cb: ToolCallback<InputArgs>
): RegisteredTool;
```

From SDK (node_modules/@modelcontextprotocol/sdk/dist/esm/spec.types.d.ts:1097-1135):
```typescript
interface ToolAnnotations {
  title?: string;
  readOnlyHint?: boolean;    // default: false
  destructiveHint?: boolean;  // default: true (worst-case!)
  idempotentHint?: boolean;   // default: false
  openWorldHint?: boolean;    // default: true (worst-case!)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace response helpers and remove listChanged capability</name>
  <files>src/mcp-server.ts</files>
  <read_first>
    - src/mcp-server.ts (current state of all tool registrations, createMcpResponse helper at lines 118-151, McpServer constructor at lines 161-165)
    - src/types.ts (ToolResponse interface definition)
    - .planning/phases/30-mcp-spec-compliance/30-PATTERNS.md (exact replacement patterns for helpers and constructor)
  </read_first>
  <action>
In src/mcp-server.ts, make three changes:

**1. Replace createMcpResponse() (lines 118-151) with mcpError() and mcpSuccess() helpers:**

Delete the entire `createMcpResponse` function (lines 118-151) and the `projectPathNotSetError` variable (lines 173-176). Replace with:

```typescript
type ErrorCode = "NOT_INITIALIZED" | "INVALID_PATH" | "BROKER_DISCONNECTED" | "NOT_FOUND" | "OPERATION_FAILED";

function mcpError(code: ErrorCode, message: string): ToolResponse {
  return {
    content: [{ type: "text", text: JSON.stringify({ ok: false, error: code, message }) }],
    isError: true,
  };
}

function mcpSuccess(data: Record<string, unknown>): ToolResponse {
  return {
    content: [{ type: "text", text: JSON.stringify({ ok: true, ...data }) }],
  };
}
```

Place these immediately after the StdioTransport class (after line 116), before the serverInfo declaration.

**2. Remove listChanged from McpServer constructor (lines 161-165):**

Change:
```typescript
const server = new McpServer(serverInfo, {
  capabilities: {
    tools: { listChanged: true }
  }
});
```
To:
```typescript
const server = new McpServer(serverInfo);
```

Per D-04: SDK registers listChanged internally on first registerTool() call. The user-space declaration is redundant.

**3. Delete the projectPathNotSetError variable (lines 173-176):**

Remove:
```typescript
const projectPathNotSetError = createMcpResponse(
  "Project path not set. Please call 'set_base_directory' to point at a different directory, or restart with --base-dir.",
  true
);
```

All usages of `projectPathNotSetError` will be replaced in Task 2 with inline `mcpError("NOT_INITIALIZED", ...)` calls.
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && grep -c "createMcpResponse" src/mcp-server.ts && grep -c "mcpError" src/mcp-server.ts && grep -c "mcpSuccess" src/mcp-server.ts && grep -c "listChanged" src/mcp-server.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/mcp-server.ts contains `function mcpError(code: ErrorCode, message: string): ToolResponse`
    - src/mcp-server.ts contains `function mcpSuccess(data: Record<string, unknown>): ToolResponse`
    - src/mcp-server.ts contains `type ErrorCode = "NOT_INITIALIZED" | "INVALID_PATH" | "BROKER_DISCONNECTED" | "NOT_FOUND" | "OPERATION_FAILED"`
    - src/mcp-server.ts does NOT contain `createMcpResponse`
    - src/mcp-server.ts does NOT contain `listChanged`
    - src/mcp-server.ts does NOT contain `projectPathNotSetError`
    - src/mcp-server.ts contains `const server = new McpServer(serverInfo);` (no second arg)
    - `npx tsc --noEmit` exits 0 (TypeScript compiles without errors)
  </acceptance_criteria>
  <done>mcpError and mcpSuccess helpers exist, createMcpResponse is deleted, listChanged is removed from constructor, projectPathNotSetError is deleted, TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 13 tools to registerTool with annotations, descriptions, and structured responses</name>
  <files>src/mcp-server.ts</files>
  <read_first>
    - src/mcp-server.ts (current state after Task 1 — verify helpers exist)
    - .planning/phases/30-mcp-spec-compliance/30-PATTERNS.md (per-tool annotation table at lines 153-167, error code mapping at lines 177-186, target patterns A/B/C)
    - .planning/phases/30-mcp-spec-compliance/30-CONTEXT.md (D-06 classification, D-07 error codes, D-08 enriched descriptions, D-09 response format)
  </read_first>
  <action>
Migrate every `server.tool(name, description, schema, callback)` call to `server.registerTool(name, config, callback)` inside the `registerTools()` function. For each tool:
- Move description from positional arg into `config.description` (enriched per D-08)
- Add `config.title` (human-readable name)
- Move inline Zod schema into `config.inputSchema` as a raw shape (NOT wrapped in z.object())
- Add `config.annotations` with exact values from D-06 classification table below
- Destructure callback params (e.g., `async (params: { filepath: string })` becomes `async ({ filepath })`)
- Replace all `return projectPathNotSetError` with `return mcpError("NOT_INITIALIZED", "Server not initialized. Call set_base_directory first or restart with --base-dir.")`
- Replace all `createMcpResponse(data)` with `mcpSuccess(data)`
- Replace all `createMcpResponse(msg, true)` with appropriate `mcpError("CODE", msg)` per the error mapping below
- Do NOT use `outputSchema` on any tool (per research recommendation — requires structuredContent return which is a breaking change)

**Per-tool annotation table (D-06):**

| Tool | readOnlyHint | destructiveHint | idempotentHint | openWorldHint |
|------|-------------|-----------------|----------------|---------------|
| set_base_directory | false | false | true | false |
| list_files | true | false | true | false |
| find_important_files | true | false | true | false |
| get_file_summary | true | false | true | false |
| set_file_summary | false | false | true | false |
| set_file_importance | false | false | true | false |
| scan_all | false | false | false | true |
| search | true | false | true | false |
| status | true | false | true | false |
| exclude_and_remove | false | true | false | false |
| detect_cycles | true | false | true | false |
| get_cycles_for_file | true | false | true | false |
| get_communities | true | false | false | false |

**Error code mapping:**

| Current pattern | Replace with |
|-----------------|--------------|
| `return projectPathNotSetError` (12 occurrences) | `return mcpError("NOT_INITIALIZED", "Server not initialized. Call set_base_directory first or restart with --base-dir.")` |
| `createMcpResponse(\`File not found: ${params.filepath}\`, true)` | `mcpError("NOT_FOUND", \`File not found in database: ${filepath}\`)` |
| `createMcpResponse(\`Failed to set file importance: \` + error, true)` | `mcpError("OPERATION_FAILED", \`Failed to set file importance: ${error}\`)` |
| `createMcpResponse(\`Failed to exclude and remove file or pattern: \` + error, true)` | `mcpError("OPERATION_FAILED", \`Failed to exclude and remove: ${error}\`)` |
| `{ content: [{ type: "text", text: "Error: Broker not connected..." }], isError: true }` (line 372) | `mcpError("BROKER_DISCONNECTED", "Broker not connected. Check LLM config in .filescope/config.json (llm.enabled must be true).")` |
| `createMcpResponse(\`No communities detected...\`, true)` | `mcpError("NOT_FOUND", "No communities detected (no local import edges).")` |
| `createMcpResponse(\`File not found in any community: ${params.file_path}\`, true)` | `mcpError("NOT_FOUND", \`File not found in any community: ${file_path}\`)` |

**Enriched descriptions (D-08) — each must state what it returns, when to use it, and preconditions:**

1. `set_base_directory`: "Override the base directory for analysis. Initializes the file watcher, database, and broker connection for the specified path. Call this first if --base-dir was not passed at startup. Subsequent calls re-initialize to a new directory."
2. `list_files`: "List all tracked files with importance rankings. Without maxItems: returns a nested directory tree structure. With maxItems: returns a flat list of the N most important files sorted by importance descending, with truncation metadata. Call status first to verify initialization."
3. `find_important_files`: "Find the highest-importance files in the project. Returns files sorted by importance descending with dependency counts and staleness flags. Use this over list_files when you need the top N files by importance with relationship metadata. Precondition: server must be initialized."
4. `get_file_summary`: "Get full intelligence for a single file: LLM-generated summary, importance score (0-10), dependency list with edge types (imports/inherits/re_exports) and confidence scores, dependents, package dependencies, concepts, change impact analysis, and staleness flags. Use this before editing a file to understand its role and relationships. Returns NOT_FOUND if the file is not in the scan database."
5. `set_file_summary`: "Manually set or override the LLM-generated summary for a file. Use when the auto-generated summary is inaccurate or you want to annotate a file with custom context. Idempotent: repeated calls with the same summary are safe. Returns NOT_FOUND if the file is not tracked."
6. `set_file_importance`: "Manually set the importance ranking (0-10) for a file. Overrides the auto-calculated importance. Falls back to basename matching if the exact path is not found. Idempotent: repeated calls with the same value are safe."
7. `scan_all`: "Queue files for LLM summarization via the broker. Uses min_importance to filter low-value files (default 1, skips zero-importance). Set remaining_only=true to skip already-summarized files. Requires an active broker connection (llm.enabled=true in config). Returns BROKER_DISCONNECTED if the broker is unreachable."
8. `search`: "Search file metadata across symbols (function/class/interface names), purpose descriptions, LLM summaries, and file paths. Returns results ranked: symbol match (100) > purpose match (50) > summary match (20) > path match (10). Use this to find files by what they do, not just their name."
9. `status`: "Get system health: broker connection state, LLM processing queue depth and progress, file watching status, and project info (root path, file count, last update time). Call this to verify initialization state and diagnose issues."
10. `exclude_and_remove`: "Permanently exclude a file or glob pattern from tracking and remove it from the database. DESTRUCTIVE: this deletes metadata and cannot be undone without a re-scan. Adds the pattern to .filescopeignore. Use for generated files, build artifacts, or false positives."
11. `detect_cycles`: "Detect all circular dependency groups in the project's local import graph. Returns an array of cycle groups (each group is an array of file paths forming a cycle), total cycle count, and total files involved. Use to identify tightly-coupled modules that may need refactoring."
12. `get_cycles_for_file`: "Get all dependency cycle groups that include a specific file. Returns only the cycles containing the specified file path, useful for understanding a single file's circular dependency involvement. Returns NOT_FOUND if the file is not tracked."
13. `get_communities`: "Get file communities detected by Louvain clustering on the local import graph. Returns groups of tightly-coupled files identified by their highest-importance representative. Optionally filter to the community containing a specific file. Communities are lazily recomputed only when the dependency graph changes."

**Special handling for set_base_directory:**
This tool delegates to `coordinator.init(params.path)` which returns its own ToolResponse. Per RESEARCH.md Pitfall 4 / Assumption A2: do NOT change coordinator.ts. Wrap the result at the handler level. After destructuring `{ path }`, call `coordinator.init(path)` and return the result. The coordinator's response format remains as-is (this tool is the one exception to the uniform response format).

**Special handling for list_files tree mode:**
When `maxItems` is undefined, the tool calls `coordinator.getFileTree()` which returns a FileNode object. This was previously wrapped by `createMcpResponse(coordinator.getFileTree())`. Now wrap it as: `return mcpSuccess({ tree: coordinator.getFileTree() })`. This wraps the tree in the uniform `{ ok: true, tree: {...} }` format.

**Special handling for exclude_and_remove init attempt:**
The current code has special logic (lines 420-429) that attempts auto-initialization from --base-dir if coordinator is not initialized. Preserve this behavior but use mcpError for the fallback error path.
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && npx tsc --noEmit 2>&1 | head -20 && grep -c 'server\.tool(' src/mcp-server.ts && grep -c 'server\.registerTool(' src/mcp-server.ts && grep -c 'annotations:' src/mcp-server.ts && grep -c 'mcpError(' src/mcp-server.ts && grep -c 'mcpSuccess(' src/mcp-server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'server\.tool(' src/mcp-server.ts` returns 0 (zero deprecated calls)
    - `grep -c 'server\.registerTool(' src/mcp-server.ts` returns 13 (all tools migrated)
    - `grep -c 'annotations:' src/mcp-server.ts` returns 13 (every tool annotated per D-05)
    - `grep -c 'readOnlyHint:' src/mcp-server.ts` returns 13 (every tool has explicit readOnlyHint)
    - `grep -c 'destructiveHint:' src/mcp-server.ts` returns 13 (every tool has explicit destructiveHint)
    - `grep -c 'openWorldHint:' src/mcp-server.ts` returns 13 (every tool has explicit openWorldHint)
    - `grep 'mcpError(' src/mcp-server.ts` shows NOT_INITIALIZED, NOT_FOUND, OPERATION_FAILED, BROKER_DISCONNECTED error codes used
    - `grep -c 'createMcpResponse' src/mcp-server.ts` returns 0 (no old helper calls)
    - `grep -c 'projectPathNotSetError' src/mcp-server.ts` returns 0 (old variable deleted)
    - src/mcp-server.ts contains `server.registerTool("set_base_directory"` through `server.registerTool("get_communities"` — all 13 tool names present
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 13 tools registered via registerTool() with title, enriched description, inputSchema raw shape, and explicit annotations. All error paths use mcpError() with structured error codes. All success paths use mcpSuccess(). Zero server.tool() calls remain. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool handler | Tool inputs arrive as JSON-RPC params over stdio; Zod inputSchema validates shape |
| tool handler -> coordinator/repository | Internal function calls; trusted boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | inputSchema raw shapes | accept | Zod schemas are unchanged from current server.tool() — same validation, moved into config object. No new attack surface. |
| T-30-02 | Information Disclosure | mcpError messages | accept | Error messages expose file paths and error codes. This is intentional for LLM agent consumption. No PII or secrets in responses. |
| T-30-03 | Denial of Service | annotations misclassification | accept | Incorrect readOnlyHint/destructiveHint could mislead MCP clients but has no security impact — annotations are advisory hints, not enforcement. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 — TypeScript compiles cleanly
2. `grep -c 'server\.tool(' src/mcp-server.ts` returns 0 — no deprecated API calls
3. `grep -c 'server\.registerTool(' src/mcp-server.ts` returns 13 — all tools migrated
4. `grep -c 'annotations:' src/mcp-server.ts` returns 13 — all tools annotated
5. `grep -c 'createMcpResponse' src/mcp-server.ts` returns 0 — old helper removed
6. `grep -c 'listChanged' src/mcp-server.ts` returns 0 — false capability removed
</verification>

<success_criteria>
All 13 MCP tools use registerTool() API with annotations, enriched descriptions, and structured error/success response format. Zero deprecated server.tool() calls. Zero createMcpResponse() references. listChanged not declared in constructor options. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-spec-compliance/30-01-SUMMARY.md`
</output>
