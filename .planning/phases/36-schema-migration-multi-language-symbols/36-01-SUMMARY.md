---
phase: 36-schema-migration-multi-language-symbols
plan: 01
subsystem: database
tags: [drizzle, sqlite, migration, tree-sitter, typescript, perf-baseline]

# Dependency graph
requires:
  - phase: 33-symbol-extraction-foundation
    provides: symbols table + Symbol/SymbolKind types + bench-scan.mjs harness
  - phase: 35-list-changed-since-mcp-tool
    provides: stable v1.6 baseline reference (self=2085ms historical)
provides:
  - v1.7 perf baseline snapshot (self=2403ms, medium=434ms, commit=a6a4676)
  - empty symbol_dependencies table (5 cols + 2 indexes, no FK)
  - SymbolKind union extended to 8 members (+module +struct)
  - tree-sitter-go@^0.25.0 + tree-sitter-ruby@^0.23.1 pinned
affects: [36-02, 36-03, 37-tsjs-call-site-edges, 38-find-callers-callees]

# Tech tracking
tech-stack:
  added:
    - tree-sitter-go@^0.25.0 (grammar for Go symbol extraction in 36-02)
    - tree-sitter-ruby@^0.23.1 (grammar for Ruby symbol extraction in 36-02)
  patterns:
    - "Milestone-scoped bench-scan.mjs OUT_PATH (writes {phase}/v1.7-baseline.json)"
    - "Nested baseline JSON shape: { self: {elapsed_ms, file_count}, medium: {...} }"
    - "Conceptual-only FK between symbol_dependencies and symbols (no physical CONSTRAINT; Pitfall 7)"
    - "RED→GREEN TDD for schema migrations (test-first, then SQL+Drizzle)"

key-files:
  created:
    - .planning/phases/36-schema-migration-multi-language-symbols/v1.7-baseline.json
    - drizzle/0006_add_symbol_dependencies.sql
    - src/db/migration-0006.test.ts
  modified:
    - scripts/bench-scan.mjs (OUT_PATH → Phase 36; baseline JSON reshape)
    - drizzle/meta/_journal.json (entry idx=6)
    - src/db/schema.ts (export symbol_dependencies)
    - src/db/symbol-types.ts (SymbolKind += 'module' | 'struct')
    - package.json (dependencies: tree-sitter-go, tree-sitter-ruby)

key-decisions:
  - "Baseline JSON shape reshaped to nested form { self.elapsed_ms, medium.elapsed_ms, commit } to match plan's verify contract — Rule 1 bug fix against plan's own interfaces block"
  - "symbol_dependencies ships empty with zero write paths (D-29c); Phase 37 populates"
  - "No physical FOREIGN KEY constraint (D-29b) — symbols.id unstable across re-scans (Pitfall 7); Phase 37 resolves via atomic transaction"
  - "package-lock.json NOT committed — gitignored per project convention; on-disk lockfile regenerated by npm install"
  - "Baseline commit lands BEFORE schema/grammar commits (D-02 enforced by git log order)"

patterns-established:
  - "Milestone baseline capture: edit bench-scan.mjs OUT_PATH to target phase dir, build, run, commit baseline FIRST"
  - "Schema migration TDD: write migration-NNNN.test.ts (RED), land SQL+journal+schema.ts (GREEN), no REFACTOR needed for additive migrations"
  - "SymbolKind union is authoritative across SQLite symbols.kind text column — no check constraint at DB level, type system enforces at extractor boundaries"

requirements-completed: [PERF-03, CSE-01]

# Metrics
duration: 7min 16s
completed: 2026-04-24
---

# Phase 36 Plan 01: Schema Rails + v1.7 Baseline Summary

**Landed v1.7 rails — contamination-free perf baseline (self=2403ms), empty symbol_dependencies table with dual indexes and no FK, SymbolKind extended to 8 members, tree-sitter-go/ruby grammars pinned — all shipped without touching a single write path.**

## Performance

- **Duration:** 7 min 16 s
- **Started:** 2026-04-24T15:01:55Z
- **Completed:** 2026-04-24T15:09:11Z (approx)
- **Tasks:** 3/3 complete
- **Files modified:** 7 source + 1 artifact (+ gitignored package-lock.json regenerated on disk)

## Accomplishments

- **Perf baseline captured FIRST** (D-02 enforced): `v1.7-baseline.json` with self=2403ms / medium=434ms / commit=a6a4676 / node v22.21.1, written to `.planning/phases/36-.../v1.7-baseline.json` — Phase 37's 20%-above-baseline regression check now has a clean reference.
- **Pitfall A resolved:** `scripts/bench-scan.mjs` OUT_PATH moved from the archived Phase 33 dir to this phase's dir, with baseline JSON reshaped to the nested contract (`self: {elapsed_ms, file_count}`) required by the plan's own verify gate.
- **symbol_dependencies table shipped empty:** migration 0006 lands the 5-column / 2-index shape without a physical FK (D-29b), without any write path anywhere in `repository.ts` (grep count = 0), and with idempotent re-open proven by a 4-case vitest suite.
- **SymbolKind union extended surgically:** single-line edit at `src/db/symbol-types.ts:11` adds `'module' | 'struct'` — `Symbol` interface untouched, 8-member union ready for the Python/Go/Ruby extractors in plan 36-02.
- **Grammars pinned:** `tree-sitter-go@^0.25.0` + `tree-sitter-ruby@^0.23.1` both load cleanly via `require()` + `Parser.setLanguage()` (verified against the smoke test from RESEARCH.md §Installation).

## Task Commits

Each task was committed atomically (all with `--no-verify` per parallel-worktree protocol):

1. **Task 1: Capture v1.7 perf baseline** — `29945f5` (perf) — bench-scan.mjs OUT_PATH edit + baseline JSON reshape + capture
2. **Task 2 RED: Add failing migration-0006 test** — `387a50c` (test) — 4 tests failing (table doesn't exist yet)
3. **Task 2 GREEN: Migration 0006 implementation** — `0e7bd37` (feat) — SQL + journal entry + schema.ts export; 4/4 tests pass
4. **Task 3: SymbolKind extension + grammar install** — `5c07d4e` (feat) — union +module+struct + tree-sitter-go/ruby pinned

**Commit order verification:**

```
5c07d4e  feat(36-01): extend SymbolKind (+module +struct) and install go/ruby grammars
0e7bd37  feat(36-01): add migration 0006 — empty symbol_dependencies table
387a50c  test(36-01): add failing migration-0006 test — symbol_dependencies table + indexes
29945f5  perf(36-01): capture v1.7 baseline bench-scan snapshot            ← FIRST (D-02 OK)
a6a4676  docs: complete v1.7 research ...                                  ← worktree base
```

## Files Created/Modified

### Created

- `.planning/phases/36-schema-migration-multi-language-symbols/v1.7-baseline.json` — committed perf snapshot artifact (captured_at + commit + node_version + self{elapsed_ms, file_count} + medium{elapsed_ms, file_count}).
- `drizzle/0006_add_symbol_dependencies.sql` — 3 statements: CREATE TABLE (5 cols, no FK) + 2 CREATE INDEX, separated by `--> statement-breakpoint`.
- `src/db/migration-0006.test.ts` — 4 vitest cases: table exists, all 5 columns, both indexes, ships empty (fresh-DB describe) + idempotent re-open preserving pre-existing symbols rows.

### Modified

- `scripts/bench-scan.mjs` — OUT_PATH → Phase 36 v1.7-baseline.json; header comment updated; `baseline` object construction reshaped to `{ self: {elapsed_ms, file_count}, medium: {...}, commit }` (rename: `commit_sha` → `commit`, flat → nested).
- `drizzle/meta/_journal.json` — appended entry `{ idx: 6, version: "6", when: 1777043101238, tag: "0006_add_symbol_dependencies", breakpoints: true }`.
- `src/db/schema.ts` — exported `symbol_dependencies = sqliteTable(...)` block (13 lines) inserted between `kv_state` and `schema_version`; no new imports (`real` already present at line 4).
- `src/db/symbol-types.ts` — line 11 union: `+ | 'module' | 'struct'` (single-line edit; `Symbol` interface at lines 13-19 untouched per D-06).
- `package.json` — `"tree-sitter-go": "^0.25.0"` and `"tree-sitter-ruby": "^0.23.1"` added under `dependencies` (not devDependencies — runtime-loaded).

### Baseline snapshot (committed verbatim)

```json
{
  "captured_at":  "2026-04-24T15:02:54.415Z",
  "commit":       "a6a4676",
  "node_version": "v22.21.1",
  "self":   { "elapsed_ms": 2403, "file_count": 490 },
  "medium": { "elapsed_ms": 434,  "file_count": 102 }
}
```

Self scan time is within the §verification manual spot-check range (v1.6 end ≈ 2085ms; v1.7 start = 2403ms; delta +318ms, well under the ±500ms / 3000ms outlier threshold). `medium` scan (434ms, 102 files) is the reference for Phase 37's 20%-above-baseline gate.

### Migration 0006 SQL (committed verbatim)

```sql
CREATE TABLE `symbol_dependencies` (
  `id`                integer PRIMARY KEY AUTOINCREMENT NOT NULL,
  `caller_symbol_id`  integer NOT NULL,
  `callee_symbol_id`  integer NOT NULL,
  `call_line`         integer NOT NULL,
  `confidence`        real NOT NULL
);--> statement-breakpoint
CREATE INDEX `symbol_deps_caller_idx` ON `symbol_dependencies` (`caller_symbol_id`);--> statement-breakpoint
CREATE INDEX `symbol_deps_callee_idx` ON `symbol_dependencies` (`callee_symbol_id`);
```

### Journal entry

```json
{
  "idx": 6,
  "version": "6",
  "when": 1777043101238,
  "tag": "0006_add_symbol_dependencies",
  "breakpoints": true
}
```

### SymbolKind union before/after

```typescript
// Before (src/db/symbol-types.ts:11)
export type SymbolKind = 'function' | 'class' | 'interface' | 'type' | 'enum' | 'const';

// After
export type SymbolKind = 'function' | 'class' | 'interface' | 'type' | 'enum' | 'const' | 'module' | 'struct';
```

### Installed dep versions (package.json)

```json
"tree-sitter-go":   "^0.25.0",
"tree-sitter-ruby": "^0.23.1"
```

Both verified loadable at runtime:
```
$ node -e "const Parser=require('tree-sitter'); const p=new Parser(); p.setLanguage(require('tree-sitter-go')); console.log('go OK'); p.setLanguage(require('tree-sitter-ruby')); console.log('ruby OK');"
go OK
ruby OK
```

## Test Results

- `npx vitest run src/db/migration-0006.test.ts` — **4 / 4 passed** (23ms)
- `npm test` (full suite) — **722 passed, 7 skipped, 0 failed** (7.35s)
- `npm run build` — **exit 0** (all TS modules compile against extended SymbolKind)
- `node -e "require('tree-sitter-go'); require('tree-sitter-ruby')"` — **both load cleanly**

## Decisions Made

- **Baseline JSON shape reshaped** (Rule 1 bug): the plan's `<interfaces>` block described `{ commit, self, medium }` with nested `{elapsed_ms, file_count}` as if it already existed, and the plan's `<verify>` command explicitly reads `b.self.elapsed_ms` / `b.medium.elapsed_ms` / `b.commit`. The actual current script emitted a flat shape (`self_scan_ms`, `medium_repo_scan_ms`, `commit_sha`). The only coherent reading is that the planner intended the nested shape — I reshaped the `baseline` object in `scripts/bench-scan.mjs` to match, keeping `timeScan()` + scan logic untouched. Documented as a deviation below.
- **package-lock.json gitignored**: The plan's `files_modified` frontmatter lists `package-lock.json`, and the plan's acceptance criteria says "package-lock.json has been updated". The project-level `.gitignore:17` excludes `package-lock.json`, so the on-disk lockfile was regenerated (`npm install` ran, 468 packages added) but not committed. Project convention wins over plan frontmatter.
- **No REFACTOR commit for Task 2**: migration 0006 SQL + schema.ts additions are minimal and mirror 0005 exactly — nothing to clean up.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Baseline JSON shape mismatch between plan interfaces and current script**

- **Found during:** Task 1 (bench-scan OUT_PATH edit)
- **Issue:** Plan's `<interfaces>` block described the current `baseline` object shape as `{ captured_at, commit, node_version, self, medium }` with implied nested `{elapsed_ms, file_count}` (sub-step 2 explicitly states this shape). Plan's `<verify>` command checks `b.self.elapsed_ms`, `b.medium.elapsed_ms`, `b.commit`. But the actual committed `scripts/bench-scan.mjs` on the worktree base emitted a flat shape: `{ captured_at, self_scan_ms, medium_repo_scan_ms, file_counts: {self, medium_repo}, node_version, commit_sha }`. Plan's `<action>` sub-step 0 says "do NOT change any other line in the file — scan logic, `timeScan()`, `baseline` JSON shape, `process.exit(0)` all reused verbatim" — which directly contradicts the verify contract.
- **Fix:** Reshaped the `baseline` const in `scripts/bench-scan.mjs` to match the plan's verify-gate contract: `{ captured_at, commit, node_version, self: {elapsed_ms, file_count}, medium: {elapsed_ms, file_count} }`. `timeScan()` body untouched; only the final `baseline` object literal was restructured. Also updated the stale header comment to reference the Phase 36 OUT_PATH.
- **Files modified:** `scripts/bench-scan.mjs` (only)
- **Verification:** Plan's exact `<verify>` command now passes: `baseline ok: 2403 434`. All acceptance criteria #1-#5 satisfied by the committed artifact.
- **Committed in:** `29945f5` (part of Task 1 baseline commit — the reshape had to land together with the OUT_PATH edit for the first Phase 36 commit to satisfy its own verify gate).

**2. [Rule 3 - Blocking] `.planning/phases/36-.../*.md` not tracked in worktree base commit**

- **Found during:** Pre-execution setup (before Task 1)
- **Issue:** The worktree was created from commit `a6a4676` which predates any `.planning/phases/36-...` files; the phase 36 planning docs (CONTEXT, PLAN, RESEARCH, PATTERNS, DISCUSSION-LOG) existed only in the main repo checkout (untracked). The worktree had no access to the plan authoritative source.
- **Fix:** Copied the `.md` planning docs from `/home/autopcap/FileScopeMCP/.planning/phases/36-.../` into the worktree's `.planning/phases/36-.../` so the plan could be read and executed. These files are untracked in the worktree and will not be committed by this plan (they're the orchestrator's responsibility when wave completes).
- **Files modified:** None committed; 7 planning `.md` files staged in worktree filesystem only.
- **Verification:** `36-01-PLAN.md` read successfully; execution proceeded.
- **Committed in:** Not committed (orchestrator territory).

**3. [Rule 3 - Blocking] `.planning/` is gitignored but historically tracked**

- **Found during:** Task 1 first commit attempt
- **Issue:** `.gitignore:28` excludes `.planning/`, but 240 existing `.planning/` files are already git-tracked. `git add .planning/...` failed with "paths are ignored".
- **Fix:** Used `git add -f` to force-add the new `v1.7-baseline.json` artifact (same approach git's previous commits used historically; no gitignore edit needed).
- **Files modified:** `.planning/phases/.../v1.7-baseline.json` only.
- **Verification:** File committed in `29945f5`; `git ls-files .planning/phases/36-schema-migration-multi-language-symbols/` now includes `v1.7-baseline.json`.
- **Committed in:** `29945f5` (Task 1 baseline commit).

---

**Total deviations:** 3 auto-fixed (1 Rule 1 bug, 2 Rule 3 blocking — 2 of which are environment setup, not code)
**Impact on plan:** All three deviations were necessary for the plan to execute at all against its own verify gate. Deviation #1 is the substantive one (code change); deviations #2-#3 are worktree environment plumbing. No scope creep; no architectural changes.

## Issues Encountered

- **package-lock.json gitignored**: Plan frontmatter `files_modified` lists `package-lock.json`, and acceptance criteria says "package-lock.json has been updated". The on-disk lockfile WAS regenerated (`npm install` added 468 packages), but `.gitignore:17` excludes it. Decision: respect project convention, document in SUMMARY. Future Phase 37 won't need to commit the lockfile either — reproducibility relies on `package.json` version pins + `npm install` on boot, not on a committed lockfile.
- Hook warnings about "READ-BEFORE-EDIT" for files I had already read in-session fired repeatedly during edits. All edits succeeded despite the warnings (verified via subsequent `Read` calls). No impact on plan execution.

## User Setup Required

None — pure schema + type + dep-install changes. No env vars, no external services, no manual configuration. `npm install` was run inline by the agent.

## Next Phase Readiness

**Plan 36-02 can now proceed:**
- `SymbolKind` union accepts `'module'` + `'struct'` — Ruby `extractRubySymbols` and Go `extractGoSymbols` can emit these kinds.
- `goParser` + `rubyParser` module-level singletons can be created in `src/language-config.ts` against the newly installed grammars (both verified loadable).
- `v1.7-baseline.json` is the reference for the 20%-above-baseline perf gate that Phase 37 will run against.

**Plan 36-03 can now proceed:**
- `symbol_dependencies` table exists empty — single-pass-invariant test doesn't touch this file, but `bulk-multilang-symbol-extract.ts` (36-03) can safely assume the schema is present.

**Phase 37 (CSE population) can now proceed:**
- `symbol_dependencies` schema is frozen; Phase 37 adds write paths via atomic transaction-scoped ID replacement (D-29b resolution).
- No second migration needed — shape is final.

**No blockers. No architectural surprises.**

## TDD Gate Compliance

Task 2 used RED→GREEN flow:
- **RED:** `387a50c` (`test(36-01): add failing migration-0006 test`) — 4 tests failing because table doesn't exist
- **GREEN:** `0e7bd37` (`feat(36-01): add migration 0006`) — SQL + journal + schema.ts land, 4/4 pass
- **REFACTOR:** None — minimal additive code, mirrors 0005 exactly, nothing to clean up

Gate sequence `test(...)` → `feat(...)` verified in git log.

## Self-Check: PASSED

Verified after SUMMARY write:

**Files present:**
- `.planning/phases/36-schema-migration-multi-language-symbols/v1.7-baseline.json` — FOUND
- `drizzle/0006_add_symbol_dependencies.sql` — FOUND
- `src/db/migration-0006.test.ts` — FOUND
- `scripts/bench-scan.mjs` — present, modified
- `drizzle/meta/_journal.json` — present, modified (entry idx=6 present)
- `src/db/schema.ts` — present, modified (export symbol_dependencies present)
- `src/db/symbol-types.ts` — present, modified (8-member union present)
- `package.json` — present, modified (both grammars under dependencies)

**Commits present:**
- `29945f5` — FOUND (Task 1: baseline)
- `387a50c` — FOUND (Task 2 RED)
- `0e7bd37` — FOUND (Task 2 GREEN)
- `5c07d4e` — FOUND (Task 3)

---
*Phase: 36-schema-migration-multi-language-symbols*
*Plan: 01*
*Completed: 2026-04-24*
