---
phase: 09-verification-documentation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CHNG-01
  - CHNG-02
  - CHNG-03
  - CHNG-04
  - CHNG-05
  - CASC-01
  - CASC-02
  - CASC-03
  - CASC-04
  - CASC-05
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06
  - LLM-07
  - LLM-08
  - COMPAT-02

must_haves:
  truths:
    - "All 18 previously-partial requirements are marked complete in REQUIREMENTS.md"
    - "Traceability table shows Complete (09) for all 18 requirements"
    - "Coverage summary shows 28/28 satisfied"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes and traceability table"
      contains: "Complete (09)"
  key_links:
    - from: "VERIFICATION.md files (Plan 01)"
      to: "REQUIREMENTS.md traceability table"
      via: "status update from Pending to Complete (09)"
      pattern: "Complete \\(09\\)"
---

<objective>
Update REQUIREMENTS.md to mark all 18 previously-partial requirements as complete, updating checkboxes, traceability table status, and coverage summary counts.

Purpose: Finalize the requirements traceability after all VERIFICATION.md files are written, bringing all v1 requirements to Complete status.
Output: Updated REQUIREMENTS.md with all 28 requirements marked complete.
</objective>

<execution_context>
@/home/autopcap/.claude/get-shit-done/workflows/execute-plan.md
@/home/autopcap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-verification-documentation/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md checkboxes and traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read the current REQUIREMENTS.md and make three sets of changes:

**1. Checkboxes — change `[ ]` to `[x]` for these 18 requirements:**
- CHNG-01, CHNG-02, CHNG-04, CHNG-05 (CHNG-03 is already `[x]`)
- CASC-01, CASC-02, CASC-03, CASC-04, CASC-05
- LLM-01, LLM-04, LLM-05, LLM-08 (LLM-02, LLM-03, LLM-06, LLM-07 are already `[x]`)
- COMPAT-02

**2. Traceability table — update status column for all 18 requirements:**
Change each row from `Pending (verification)` or `Pending (integration fix + verification)` to `Complete (09)`:
- CHNG-01: `Complete (09)`
- CHNG-02: `Complete (09)`
- CHNG-03: `Complete (09)` (was "Pending (integration fix + verification)")
- CHNG-04: `Complete (09)`
- CHNG-05: `Complete (09)`
- CASC-01 through CASC-05: `Complete (09)`
- LLM-01: `Complete (09)`
- LLM-02: `Complete (09)` (was "Pending (integration fix + verification)")
- LLM-03: `Complete (09)` (was "Pending (integration fix + verification)")
- LLM-04: `Complete (09)`
- LLM-05: `Complete (09)`
- LLM-06: `Complete (09)` (was "Pending (integration fix + verification)")
- LLM-07: `Complete (09)` (was "Pending (integration fix + verification)")
- LLM-08: `Complete (09)`
- COMPAT-02: `Complete (09)`

**3. Coverage summary — update counts at bottom:**
- Change `Satisfied (verified): 10` to `Satisfied (verified): 28`
- Change `Pending (verification/fix): 18` to `Pending (verification/fix): 0`
- Update `Last updated` date to today

Do NOT change anything else in the file. Do NOT modify v2 requirements, out of scope section, or any other content.
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/REQUIREMENTS.md && grep -c "Complete (09)" .planning/REQUIREMENTS.md && grep "Satisfied" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All 28 v1 requirement checkboxes are [x]. All 18 previously-pending requirements show "Complete (09)" in the traceability table. Coverage summary shows 28 satisfied, 0 pending.</done>
</task>

</tasks>

<verification>
1. `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns 0 for v1 requirements (only v2 has unchecked boxes)
2. `grep "Pending (verification)" .planning/REQUIREMENTS.md` returns no matches
3. `grep "Satisfied (verified): 28" .planning/REQUIREMENTS.md` returns a match
4. `grep "Pending (verification/fix): 0" .planning/REQUIREMENTS.md` returns a match
</verification>

<success_criteria>
- All 28 v1 requirements have [x] checkboxes
- All 18 traceability table rows updated to "Complete (09)"
- Coverage summary: 28 satisfied, 0 pending
- No unrelated content modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-documentation/09-02-SUMMARY.md`
</output>
