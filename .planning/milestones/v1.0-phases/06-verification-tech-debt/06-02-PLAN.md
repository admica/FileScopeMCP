---
phase: 06-verification-tech-debt
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .planning/phases/01-sqlite-storage/01-VERIFICATION.md
  - .planning/phases/02-coordinator-daemon-mode/02-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-06
  - COMPAT-01
  - COMPAT-03

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 1 with evidence for STOR-01, STOR-02, STOR-03, STOR-04, STOR-07, COMPAT-01"
    - "VERIFICATION.md exists for Phase 2 with evidence for STOR-05, STOR-06, COMPAT-03"
    - "Each requirement cites specific test file and test name as evidence"
    - "REQUIREMENTS.md traceability table shows all 9 requirements as Complete"
    - "Any requirement lacking test coverage has a new integration test added"
  artifacts:
    - path: ".planning/phases/01-sqlite-storage/01-VERIFICATION.md"
      provides: "Phase 1 requirement verification evidence"
      contains: "STOR-01"
    - path: ".planning/phases/02-coordinator-daemon-mode/02-VERIFICATION.md"
      provides: "Phase 2 requirement verification evidence"
      contains: "STOR-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability marking 9 requirements Complete"
      contains: "Complete (06)"
  key_links:
    - from: ".planning/phases/01-sqlite-storage/01-VERIFICATION.md"
      to: "src/db/db.test.ts"
      via: "Evidence references to test names"
      pattern: "STOR-01.*VERIFIED"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/06-verification-tech-debt/06-02-PLAN.md"
      via: "Traceability updated to Complete (06)"
      pattern: "Complete.*06"
---

<objective>
Create VERIFICATION.md files for Phase 1 (SQLite Storage) and Phase 2 (Coordinator + Daemon Mode), formally closing 9 partial requirements. Run existing tests to confirm all pass, map each requirement to its test evidence, fill any coverage gaps with minimal integration tests, and update REQUIREMENTS.md traceability.

Purpose: Close the 9 requirements that were implemented in Phases 1-2 but never formally verified. Future Claude sessions can quickly see "what's done" without re-reading Phase 1/2 code.
Output: 2 VERIFICATION.md files, updated REQUIREMENTS.md with all 9 requirements marked Complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-verification-tech-debt/06-CONTEXT.md
@.planning/phases/06-verification-tech-debt/06-RESEARCH.md
@.planning/phases/06-verification-tech-debt/06-01-SUMMARY.md

@src/db/db.test.ts
@src/db/repository.test.ts
@src/migrate/json-to-sqlite.test.ts
@src/coordinator.test.ts
@src/mcp-server.test.ts

<interfaces>
<!-- Requirement-to-test mapping from RESEARCH.md -->

Phase 1 Requirements:
- STOR-01 (SQLite storage): db.test.ts "opens DB, applies WAL mode pragmas" + repository.test.ts "upsertFile > inserts and retrieves all FileNode fields"
- STOR-02 (JSON migration): json-to-sqlite.test.ts "triggers migration when JSON file exists and no DB present" + coordinator.test.ts "init() runs migration for existing JSON tree"
- STOR-03 (Schema supports staleness/deps/metadata): db.test.ts WAL + pragma checks; repository.test.ts schema validation; staleness columns in schema
- STOR-04 (MCP tools backward compat): mcp-server.test.ts staleness injection tests
- STOR-07 (LLM jobs persist): repository.ts llm_jobs table; pipeline.test.ts restart tests
- COMPAT-01 (20+ MCP tools identical): mcp-server.test.ts tool registration tests

Phase 2 Requirements:
- STOR-05 (Coordinator standalone): coordinator.test.ts "Daemon mode: init() runs standalone without initServer() or MCP transport"
- STOR-06 (--daemon flag): coordinator.test.ts PID guard tests + daemon mode test
- COMPAT-03 (Works without LLM): coordinator.test.ts "works with no LLM configured (COMPAT-03)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests, identify gaps, fill coverage gaps with minimal integration tests</name>
  <files>src/db/db.test.ts, src/db/repository.test.ts, src/migrate/json-to-sqlite.test.ts, src/coordinator.test.ts, src/mcp-server.test.ts</files>
  <action>
1. **Run the full test suite** and capture output: `npx vitest run 2>&1`. Confirm all tests pass.

2. **Map each of the 9 requirements to existing test evidence.** For each requirement, find the specific `describe > it/test` block that proves it. Use the mapping from RESEARCH.md as a starting point but verify by reading the actual test files:
   - STOR-01: Look for tests that verify SQLite is used for metadata storage
   - STOR-02: Look for tests that verify JSON-to-SQLite migration triggers automatically
   - STOR-03: Look for tests that verify staleness columns, dependency join table, metadata fields exist in schema
   - STOR-04: Look for tests that verify MCP tools return same shapes after migration
   - STOR-07: Look for tests that verify LLM jobs table exists and persists across operations
   - COMPAT-01: Look for tests that verify all MCP tool names and schemas are registered
   - STOR-05: Look for tests that coordinator runs without MCP transport
   - STOR-06: Look for tests that --daemon flag works with PID guard
   - COMPAT-03: Look for tests that system works without LLM configured

3. **If any requirement has NO existing test coverage**, write a minimal focused integration test in the appropriate test file. One test per gap. The test should exercise the requirement's core behavior and be clearly named (e.g., `test('STOR-03: schema has staleness columns and dependency join table')`).

4. **Run tests again** to confirm any new tests pass.
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Every one of the 9 requirements has at least one test that exercises its core behavior. All tests pass. Test-to-requirement mapping is clear.</done>
</task>

<task type="auto">
  <name>Task 2: Create VERIFICATION.md files and update REQUIREMENTS.md traceability</name>
  <files>.planning/phases/01-sqlite-storage/01-VERIFICATION.md, .planning/phases/02-coordinator-daemon-mode/02-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
1. **Create `.planning/phases/01-sqlite-storage/01-VERIFICATION.md`** with this structure:
   ```
   # Phase 1: SQLite Storage - Verification

   **Verified:** 2026-03-18
   **Test command:** `npx vitest run src/db/db.test.ts src/db/repository.test.ts src/migrate/json-to-sqlite.test.ts src/coordinator.test.ts src/mcp-server.test.ts`
   **Result:** All tests pass

   ## STOR-01: SQLite stores all file metadata, non-breaking migration
   **Status:** VERIFIED
   **Evidence:**
   - `{test_file}` -- `{describe} > {test name}`
   **Behavior confirmed:** {one sentence}

   [repeat for STOR-02, STOR-03, STOR-04, STOR-07, COMPAT-01]
   ```
   - Use actual test names found in Task 1 (not placeholder names)
   - Each evidence line: relative file path, describe block name, test name
   - Behavior confirmed: one sentence describing what the test proves

2. **Create `.planning/phases/02-coordinator-daemon-mode/02-VERIFICATION.md`** with same structure for STOR-05, STOR-06, COMPAT-03.

3. **Update `.planning/REQUIREMENTS.md` traceability table:**
   - Change all 9 requirement statuses from `Pending` to `Complete (06)`
   - STOR-01 through STOR-07: `Complete (06)`
   - COMPAT-01: `Complete (06)`
   - COMPAT-03: `Complete (06)`
   - Do NOT modify any other requirements (CHNG-*, CASC-*, LLM-*, COMPAT-02 are already correct)
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && grep -c "VERIFIED" .planning/phases/01-sqlite-storage/01-VERIFICATION.md && grep -c "VERIFIED" .planning/phases/02-coordinator-daemon-mode/02-VERIFICATION.md && grep -c "Complete (06)" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Phase 1 VERIFICATION.md has 6 VERIFIED requirements. Phase 2 VERIFICATION.md has 3 VERIFIED requirements. REQUIREMENTS.md shows 9 requirements as Complete (06). All 28 v1 requirements have a terminal status.</done>
</task>

</tasks>

<verification>
1. `cat .planning/phases/01-sqlite-storage/01-VERIFICATION.md | grep "VERIFIED" | wc -l` — returns 6
2. `cat .planning/phases/02-coordinator-daemon-mode/02-VERIFICATION.md | grep "VERIFIED" | wc -l` — returns 3
3. `grep "Pending" .planning/REQUIREMENTS.md` — returns empty (no pending requirements remain)
4. `npx vitest run` — all tests pass
</verification>

<success_criteria>
- 01-VERIFICATION.md exists with 6 verified requirements (STOR-01-04, STOR-07, COMPAT-01)
- 02-VERIFICATION.md exists with 3 verified requirements (STOR-05, STOR-06, COMPAT-03)
- Each requirement cites real test file and test name
- REQUIREMENTS.md has all 9 requirements marked Complete (06)
- Zero "Pending" requirements remain in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-tech-debt/06-02-SUMMARY.md`
</output>
