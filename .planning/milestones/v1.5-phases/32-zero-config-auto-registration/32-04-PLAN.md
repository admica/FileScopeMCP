---
phase: 32-zero-config-auto-registration
plan: 04
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
  - 32-03
files_modified:
  - README.md
  - docs/mcp-clients.md
autonomous: true
requirements:
  - ZERO-03
user_setup: []

must_haves:
  truths:
    - "README Quick Start still promises `./build.sh` as the single entry point, and now explicitly notes `./build.sh` runs `claude mcp add --scope user` (idempotent, re-runnable via `npm run register-mcp`)"
    - "README contains no reference to the deleted `install-mcp-claude.sh`"
    - "`docs/mcp-clients.md` Claude Code section describes the new flow: `npm run register-mcp` for end-users and the committed `.mcp.json` dogfood for contributors"
    - "`docs/mcp-clients.md` contains inline OS-specific JSON snippets for Cursor AI (WSL, Windows Native, macOS/Linux Native) — no references to deleted template files"
    - "`docs/mcp-clients.md` has a new `## Cross-host (WSL → Windows)` section documenting the manual `wsl -d <distro>` shim, with the JSON block preserved verbatim from the deleted `mcp.json.linux`"
    - "`docs/mcp-clients.md` section order matches D-18: Claude Code → Cursor AI → Cross-host (WSL) → Daemon Mode"
  artifacts:
    - path: "README.md"
      provides: "Quick Start section updated with registration note and no install-mcp-claude.sh reference"
      contains: "npm run register-mcp"
    - path: "docs/mcp-clients.md"
      provides: "Rewritten client setup doc with four sections in D-18 order"
      min_lines: 80
      contains: "Cross-host (WSL"
  key_links:
    - from: "README.md §Quick Start"
      to: "docs/mcp-clients.md"
      via: "hyperlink for manual setup fallback"
      pattern: "\\[docs/mcp-clients\\.md\\]\\(docs/mcp-clients\\.md\\)"
    - from: "docs/mcp-clients.md §Claude Code"
      to: "scripts/register-mcp.mjs (via `npm run register-mcp`)"
      via: "documented command"
      pattern: "npm run register-mcp"
    - from: "docs/mcp-clients.md §Cross-host"
      to: "run.sh (generated by build.sh)"
      via: "WSL wrapper JSON snippet"
      pattern: "run\\.sh"
---

<objective>
Update user-facing documentation so a developer following the setup from scratch reaches a working installation without any manual JSON editing. Rewrite `docs/mcp-clients.md` to reflect the new registration flow (Plans 01 through 03) and consolidate the OS-specific JSON snippets from the now-deleted template files. Update README Quick Start to reference `npm run register-mcp` as the underlying mechanism while preserving the `./build.sh` one-command promise.

Purpose: Implements ZERO-03. The prior plans deleted `install-mcp-claude.sh`, the four `mcp.json.*` templates, and the `mcp.json.claude-code` dogfood template. This plan updates every public doc reference so new contributors and end-users don't encounter stale instructions.

Output: One updated README.md Quick Start block, one fully rewritten `docs/mcp-clients.md` with four sections in the D-18 order.
</objective>

<execution_context>
@/home/autopcap/FileScopeMCP/.claude/get-shit-done/workflows/execute-plan.md
@/home/autopcap/FileScopeMCP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-zero-config-auto-registration/32-CONTEXT.md
@.planning/phases/32-zero-config-auto-registration/32-PATTERNS.md
@.planning/phases/32-zero-config-auto-registration/32-01-SUMMARY.md
@.planning/phases/32-zero-config-auto-registration/32-02-SUMMARY.md
@.planning/phases/32-zero-config-auto-registration/32-03-SUMMARY.md
@README.md
@docs/mcp-clients.md

<interfaces>
### README Quick Start — current block (README.md lines 26-34):
```markdown
## Quick Start

\`\`\`bash
git clone https://github.com/admica/FileScopeMCP.git
cd FileScopeMCP
./build.sh          # installs deps, compiles, registers with Claude Code
\`\`\`

That's it. Open a Claude Code session in any project and FileScopeMCP auto-initializes. Try:
```

### README Quick Start — target block (insert one sentence after the "That's it." line):
Insert between current lines 32 (blank) and 34 ("Try:"), adding a single sentence that makes the registration mechanism explicit while preserving the existing phrasing:

```markdown
`./build.sh` registers FileScopeMCP globally via `claude mcp add --scope user` (idempotent; re-run with `npm run register-mcp`). If the `claude` CLI is missing, the build still succeeds — see [docs/mcp-clients.md](docs/mcp-clients.md) for manual setup.
```

Do NOT add, remove, or reword any other README line. Do NOT reference `install-mcp-claude.sh` anywhere in README.

### `docs/mcp-clients.md` — target full content (target file body, ~90 lines):

The file is rewritten top to bottom. Section order per D-18: Claude Code → Cursor AI → Cross-host (WSL) → Daemon Mode. Exact content follows (reproduce verbatim in the Write):

```markdown
# MCP Client Setup

## Claude Code

Registered automatically by `./build.sh` via `claude mcp add --scope user` — idempotent, re-runnable. To re-register without rebuilding, run:

\`\`\`bash
npm run register-mcp
\`\`\`

This calls `claude mcp add --scope user FileScopeMCP <node> <path-to-dist/mcp-server.js>`. User scope means one registration covers every Claude Code session on the host. If the `claude` CLI is not installed, the script prints a hint and exits 0 — the build does not fail.

### Contributor dogfood

Opening the FileScopeMCP repo itself in Claude Code auto-discovers the server via the committed `.mcp.json` at the repo root:

\`\`\`json
{
  "mcpServers": {
    "FileScopeMCP": {
      "command": "node",
      "args": ["dist/mcp-server.js"]
    }
  }
}
\`\`\`

This only takes effect when Claude Code is open on this repo (its CWD is this repo root). End users do not need this file — `npm run register-mcp` covers them globally.

### Selecting a different project

FileScopeMCP auto-initializes to the CWD Claude Code starts it in. To analyze a different directory at runtime:

\`\`\`
set_base_directory(path: "/path/to/your/project")
\`\`\`

## Cursor AI

Cursor does not have a `claude mcp add` equivalent, so registration is manual. Copy the appropriate JSON block below into your project's `.cursor/mcp.json`.

### WSL (Cursor on Windows, FileScopeMCP in WSL)

Build inside WSL (`./build.sh`), then add to your project's `.cursor/mcp.json`:

\`\`\`json
{
  "mcpServers": {
    "FileScopeMCP": {
      "command": "wsl",
      "args": ["-d", "Ubuntu-24.04", "/home/yourname/FileScopeMCP/run.sh", "--base-dir=${projectRoot}"],
      "transport": "stdio",
      "disabled": false,
      "alwaysAllow": []
    }
  }
}
\`\`\`

Replace `Ubuntu-24.04` with your WSL distro name (`wsl -l -q` to list) and `/home/yourname/FileScopeMCP` with the WSL path to this repo.

### Windows Native

\`\`\`json
{
  "mcpServers": {
    "FileScopeMCP": {
      "command": "node",
      "args": ["C:\\\\FileScopeMCP\\\\dist\\\\mcp-server.js", "--base-dir=${projectRoot}"],
      "transport": "stdio",
      "disabled": false,
      "alwaysAllow": []
    }
  }
}
\`\`\`

### macOS / Linux Native

\`\`\`json
{
  "mcpServers": {
    "FileScopeMCP": {
      "command": "node",
      "args": ["/path/to/FileScopeMCP/dist/mcp-server.js", "--base-dir=${projectRoot}"],
      "transport": "stdio"
    }
  }
}
\`\`\`

## Cross-host (WSL → Windows)

If FileScopeMCP runs in WSL and Claude Code runs on the Windows host, automatic registration via `npm run register-mcp` does not apply — the `claude` CLI on Windows cannot spawn a WSL process directly. Configure this manually by editing the Windows-side `.claude.json` (or using `claude mcp add` on Windows) with a `wsl` shim:

\`\`\`json
{
  "mcpServers": {
    "FileScopeMCP": {
      "command": "wsl",
      "args": ["-d", "Ubuntu-24.04", "/home/yourname/FileScopeMCP/run.sh", "--base-dir=${projectRoot}"],
      "transport": "stdio",
      "disabled": false,
      "alwaysAllow": []
    }
  }
}
\`\`\`

Replace `Ubuntu-24.04` with your distro (`wsl -l -q`) and `/home/yourname/FileScopeMCP` with the WSL path. `run.sh` is generated by `./build.sh` in WSL and simply forwards arguments to `node dist/mcp-server.js`.

## Daemon Mode

Run FileScopeMCP as a standalone background process (no MCP client required):

\`\`\`bash
node dist/mcp-server.js --daemon --base-dir=/path/to/project
\`\`\`

Logs go to `.filescope-daemon.log` in the project root. A PID file at `.filescope/instance.pid` prevents concurrent daemons on the same project.
```

### Rules for the rewrite
- Sections in exact D-18 order: `## Claude Code`, `## Cursor AI`, `## Cross-host (WSL → Windows)`, `## Daemon Mode`.
- Cursor subsections in order: WSL → Windows Native → macOS / Linux Native (matches current file's order for the three existing ones).
- Do NOT reference any of the deleted files: `install-mcp-claude.sh`, `mcp.json.linux`, `mcp.json.mac`, `mcp.json.win.txt`, `mcp.json.claude-code`.
- Daemon Mode section content is preserved verbatim from the current `docs/mcp-clients.md` lines 67-75.
- The WSL JSON block in both Cursor §WSL and Cross-host is intentionally similar — do not de-duplicate; each section is self-contained per D-11 (one source of truth for each context).
- Windows Native JSON uses double-backslashes in the path (`C:\\\\FileScopeMCP\\\\...`) — this is JSON escaping for a Windows path literal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update README Quick Start with registration note</name>
  <files>README.md</files>
  <read_first>
    - README.md (whole file, but specifically lines 26-47 §Quick Start)
    - .planning/phases/32-zero-config-auto-registration/32-CONTEXT.md §D-17
    - .planning/phases/32-zero-config-auto-registration/32-PATTERNS.md §`README.md` §Quick Start (EDIT)
    - .planning/phases/32-zero-config-auto-registration/32-02-SUMMARY.md (confirm `npm run register-mcp` is the current entry point)
  </read_first>
  <action>
Edit `/home/autopcap/FileScopeMCP/README.md` §Quick Start. Insert exactly one new paragraph between the current line 32 (blank line after the code fence) and the "That's it. Open a Claude Code session…" paragraph (currently line 34).

Before (current lines 26-34):
```markdown
## Quick Start

\`\`\`bash
git clone https://github.com/admica/FileScopeMCP.git
cd FileScopeMCP
./build.sh          # installs deps, compiles, registers with Claude Code
\`\`\`

That's it. Open a Claude Code session in any project and FileScopeMCP auto-initializes. Try:
```

After (new content is a single paragraph after the code fence's closing backticks, before "That's it."):
```markdown
## Quick Start

\`\`\`bash
git clone https://github.com/admica/FileScopeMCP.git
cd FileScopeMCP
./build.sh          # installs deps, compiles, registers with Claude Code
\`\`\`

`./build.sh` registers FileScopeMCP globally via `claude mcp add --scope user` (idempotent; re-run with `npm run register-mcp`). If the `claude` CLI is missing, the build still succeeds — see [docs/mcp-clients.md](docs/mcp-clients.md) for manual setup.

That's it. Open a Claude Code session in any project and FileScopeMCP auto-initializes. Try:
```

Rules:
- Do NOT change the existing code fence, its contents, or the inline comment `# installs deps, compiles, registers with Claude Code` — it is already accurate.
- Do NOT change the "That's it." paragraph or anything below it.
- Do NOT reference `install-mcp-claude.sh` anywhere in README.md (verify globally with grep).
- No additional sentences, no bullet lists — exactly one paragraph of new prose.
- Do NOT modify any other part of README.md (MCP Tools table, Nexus Dashboard, How It Works, etc).

Commit as: `docs(32-04): add register-mcp note to README Quick Start`.
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && grep -qE 'npm run register-mcp' README.md && grep -qE 'claude mcp add --scope user' README.md && grep -qE 'docs/mcp-clients\.md' README.md && ! grep -qE 'install-mcp-claude' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'npm run register-mcp' README.md` returns at least one match (new registration note)
    - `grep -n 'claude mcp add --scope user' README.md` returns at least one match
    - `grep -n 'docs/mcp-clients.md' README.md` returns at least one match (the hyperlink for manual setup)
    - `grep -n 'install-mcp-claude' README.md` returns zero matches
    - The `./build.sh` line inside the code fence still exists unchanged (`grep -n './build.sh          # installs deps, compiles, registers with Claude Code' README.md` returns one match)
    - Git diff limited to the Quick Start section: `git diff --stat README.md` reports only one hunk, inserted around lines 33-34
  </acceptance_criteria>
  <done>
    README Quick Start includes the new registration paragraph. No reference to `install-mcp-claude.sh` anywhere. All other README content unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite `docs/mcp-clients.md` with four sections in D-18 order</name>
  <files>docs/mcp-clients.md</files>
  <read_first>
    - docs/mcp-clients.md (current file, 76 lines)
    - .planning/phases/32-zero-config-auto-registration/32-CONTEXT.md §D-11, D-15, D-18
    - .planning/phases/32-zero-config-auto-registration/32-PATTERNS.md §`docs/mcp-clients.md` (EDIT — full rewrite per D-18) — the verbatim JSON block for the Cross-host section is there
    - .planning/phases/32-zero-config-auto-registration/32-01-SUMMARY.md (confirm `.mcp.json` body and .gitignore state)
    - .planning/phases/32-zero-config-auto-registration/32-02-SUMMARY.md (confirm `npm run register-mcp` command)
    - .planning/phases/32-zero-config-auto-registration/32-03-SUMMARY.md (confirm legacy files are deleted)
  </read_first>
  <action>
Replace the entire contents of `/home/autopcap/FileScopeMCP/docs/mcp-clients.md` with the four-section rewrite specified verbatim in the `<interfaces>` block above. Section order is mandatory per D-18:

1. `## Claude Code` — describes `npm run register-mcp` (primary) and the `.mcp.json` contributor-dogfood (secondary subsection `### Contributor dogfood`). Mentions `--scope user` semantics, ENOENT fail-soft, and `set_base_directory` at runtime.
2. `## Cursor AI` — three OS subsections in order WSL, Windows Native, macOS / Linux Native. JSON blocks are self-contained (Cursor does not use `claude mcp add`). Windows Native uses double-backslash JSON escaping for the Windows path.
3. `## Cross-host (WSL → Windows)` — NEW section per D-15 and D-18. Manual config only. JSON block is the same WSL shim shape as Cursor §WSL, with prose explaining why automation does not apply (Windows `claude` CLI cannot spawn a WSL process directly). Content is sourced from the pre-deletion `mcp.json.linux` template — the body is captured verbatim in `32-PATTERNS.md` §`docs/mcp-clients.md`.
4. `## Daemon Mode` — preserved verbatim from the current file's lines 67-75 (orthogonal to registration; do not edit).

Constraints:
- No references to `install-mcp-claude.sh`, `mcp.json.linux`, `mcp.json.mac`, `mcp.json.win.txt`, or `mcp.json.claude-code` anywhere in the file.
- Reference `dist/mcp-server.js` and `run.sh` as-needed; both are still produced by `./build.sh`.
- Use fenced JSON (```json) for JSON blocks, fenced bash (```bash) for shell commands, fenced text (no language) for MCP tool invocations like `set_base_directory(...)`.
- The final file length is between 80 and 120 lines (the `<interfaces>` block above is ~90 lines of target content including blank lines).
- Do not introduce HTML, collapsible `<details>` blocks, images, or footnote-style references.

Commit as: `docs(32-04): rewrite mcp-clients.md around npm run register-mcp and inline snippets`.
  </action>
  <verify>
    <automated>cd /home/autopcap/FileScopeMCP && grep -qE '^## Claude Code' docs/mcp-clients.md && grep -qE '^## Cursor AI' docs/mcp-clients.md && grep -qE '^## Cross-host' docs/mcp-clients.md && grep -qE '^## Daemon Mode' docs/mcp-clients.md && grep -qE 'npm run register-mcp' docs/mcp-clients.md && grep -qE 'claude mcp add --scope user' docs/mcp-clients.md && grep -qE '"command": "wsl"' docs/mcp-clients.md && grep -qE '--daemon' docs/mcp-clients.md && ! grep -qE 'install-mcp-claude' docs/mcp-clients.md && ! grep -qE 'mcp\.json\.(linux|mac|win\.txt|claude-code)' docs/mcp-clients.md && awk '/^## Claude Code/{c=NR} /^## Cursor AI/{r=NR} /^## Cross-host/{x=NR} /^## Daemon Mode/{d=NR} END{exit (c<r && r<x && x<d)?0:1}' docs/mcp-clients.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains all four section headers at `##` level: `## Claude Code`, `## Cursor AI`, `## Cross-host (WSL → Windows)`, `## Daemon Mode`
    - Headers appear in that exact order top-to-bottom (awk line-number check confirms)
    - `## Claude Code` section contains both `npm run register-mcp` and `claude mcp add --scope user`
    - `## Claude Code` contains a `### Contributor dogfood` subsection that shows the `.mcp.json` contents with relative path `dist/mcp-server.js`
    - `## Cursor AI` contains three OS subsections in order: WSL, Windows Native, macOS / Linux Native
    - `## Cross-host (WSL → Windows)` contains a JSON block with `"command": "wsl"` and references `run.sh`
    - `## Daemon Mode` contains `--daemon` and `--base-dir=` invocation
    - Zero matches for `install-mcp-claude` anywhere in the file
    - Zero matches for any `mcp.json.linux`, `mcp.json.mac`, `mcp.json.win.txt`, `mcp.json.claude-code` filename reference
    - File is valid Markdown: `grep -cE '^```' docs/mcp-clients.md` returns an even number (balanced fences)
    - File length is between 80 and 120 lines (`wc -l docs/mcp-clients.md`)
  </acceptance_criteria>
  <done>
    `docs/mcp-clients.md` is rewritten, four sections in D-18 order, all required substrings present, zero references to deleted files, balanced code fences.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer reading docs → follow setup steps | A stale doc reference to `install-mcp-claude.sh` would send the user to a deleted file, breaking setup |
| copy-paste of JSON blocks → Claude Code / Cursor config | Incorrect JSON in docs becomes incorrect config on every user's disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-15 | Tampering | Stale doc reference to deleted script | mitigate | Automated grep (`grep -qE 'install-mcp-claude' docs/mcp-clients.md` returns non-zero) asserts zero references in both README and docs/mcp-clients.md before plan is considered done. |
| T-32-16 | Information Disclosure | Absolute paths or secrets in doc examples | mitigate | Doc examples use placeholder paths (`/path/to/FileScopeMCP`, `C:\\FileScopeMCP\\...`, `/home/yourname/FileScopeMCP`) rather than any contributor-specific path. No secrets, tokens, or environment variables appear in the doc. |
| T-32-17 | Tampering | Incorrect JSON snippets copy-pasted | mitigate | Each JSON block is copied verbatim from the pre-Phase-32 `docs/mcp-clients.md` (Cursor OS blocks) or from `mcp.json.linux` (Cross-host block) via the verbatim capture in `32-PATTERNS.md`. No new JSON authored in this plan — only consolidation. |
| T-32-18 | Denial of Service | Malformed Markdown breaks rendering | mitigate | Balanced-fence check in verify block (`grep -cE '^\`\`\`'` returns even number) catches unmatched code fences. Manual preview during execute is encouraged. |
</threat_model>

<verification>
- README.md contains `npm run register-mcp` and `claude mcp add --scope user` references at least once each.
- Neither README.md nor docs/mcp-clients.md references `install-mcp-claude` or any `mcp.json.linux|mac|win.txt|claude-code` filename.
- docs/mcp-clients.md has four sections in the D-18 order, each with the expected content markers.
- Balanced code fences throughout docs/mcp-clients.md.
</verification>

<success_criteria>
1. ZERO-03 satisfied: a developer following README from scratch reaches a working install with no manual JSON editing (primary path) or follows `docs/mcp-clients.md` for manual fallbacks.
2. D-17 satisfied: README Quick Start notes the mechanism (`claude mcp add --scope user`, idempotent, re-runnable).
3. D-18 satisfied: docs/mcp-clients.md rewritten with four sections in the specified order and inline Cursor JSON snippets (no external template files).
4. D-11 satisfied: each client-context JSON block lives in exactly one place (no duplication across `mcp.json.*` template files).
5. Every reference to a deleted artifact (`install-mcp-claude.sh`, `mcp.json.linux`, `mcp.json.mac`, `mcp.json.win.txt`, `mcp.json.claude-code`) is gone from all user-facing docs.
</success_criteria>

<output>
After completion, create `.planning/phases/32-zero-config-auto-registration/32-04-SUMMARY.md`.
</output>
